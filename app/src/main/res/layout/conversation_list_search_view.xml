<org.thoughtcrime.securesms.components.Mp02EditText xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/search"
    android:layout_width="match_parent"
    android:layout_height="@dimen/conversation_list_item_default_height_focused"
    android:background="@null"
    android:focusable="true"
    android:gravity="left|center_vertical"
    android:hint="@string/SearchToolbar_search"
    android:inputType="textEmailAddress"
    android:maxLines="1"
    android:paddingLeft="5dp"
    android:textColor="@drawable/mp02_list_text_color"
    android:textColorHint="#40ffffff"
    android:textCursorDrawable="@drawable/mp02_cursor_style"
    android:textSize="40sp" />